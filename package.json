{
  "name": "mirage-partner",
  "version": "1.0.0",
  "description": "Mirage Studios — main site + per-client proposal microsites.",
  "private": true,
  "main": "server.js",
  "scripts": {
    "build": "cd frontend && npm install && npm run build && rm -rf ../public && cp -r dist ../public",
    "start": "node server.js",
    "dev": "node server.js"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.90.0",
    "bcrypt": "^6.0.0",
    "better-sqlite3": "^12.9.0",
    "cookie-parser": "^1.4.6",
    "express": "^4.19.2",
    "multer": "^2.1.1"
  }
}
